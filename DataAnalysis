# Code For Sub-Research Question 1 
# Data Cleaning 

# We compare the overall crime rate in London boroughs to sexual offenses per London borough 

# We first import the data
import pandas as pd
df1 = pd.read_csv(r'/content/data/MPS Borough Level Crime (24 months).csv',index_col=0)

# Add comment on what was done 
df1.drop (['LookUp_BoroughName'], axis=1, inplace=True)
df1=df1.reset_index(drop=True) 
df1.columns.values[0]="MONTH"
#df1 = df1.set_index('MinorText', drop=True).rename_axis(None)
pd.options.display.max_rows = 198
df1.head(198)

# Sum all the values for each crime for every month
df1= df1.groupby(by='MONTH').sum()

# We transpose our dataset to obtain all the variables would be in columns (columns=XXX and lines=XXX)
df1.T.head()
